# MongoDB
MONGO_URL=sua_url_do_mongodb

# API DeepSeek
DEEPSICK_API=sua_chave_api_deepseek

# Ambiente (development ou production)
NODE_ENV=production

# URL do frontend (para CORS em produção)
FRONTEND_URL=https://seu-frontend.onrender.com

# Porta (não precisa configurar no Render, ele define automaticamente)
# PORT=3000
